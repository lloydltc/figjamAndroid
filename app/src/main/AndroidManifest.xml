<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.figjam">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FigJam"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".Views.Activity.DetailsActivity"
            android:exported="false" />
        <activity
            android:name=".Views.Activity.UpdateRecord"
            android:exported="false" />
        <activity
            android:name=".Views.Activity.LoadFromDatabaseActivity"
            android:exported="false" />
        <activity
            android:name=".Views.Activity.CreateTypeCodeUser"
            android:exported="false" />
        <activity
            android:name=".Views.Activity.FingerPrintActivity"
            android:exported="false" />
        <activity
            android:name=".Views.Activity.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".Views.Activity.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".Views.Activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>